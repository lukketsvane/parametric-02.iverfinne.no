# parametric-02.iverfinne.no

A parametric 3D studio. The UI shell (viewer stage, lighting, gestures,
controls panel) comes from parametric-01.iverfinne.no; the generative
motor was removed and a new one will be built here. The viewer currently
shows a placeholder mesh — see components/viewer.tsx for where real
geometry mounts.
